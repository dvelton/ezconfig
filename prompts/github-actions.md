# Set Up GitHub Actions Automation

Paste this into your AI tool when you want to automate something that runs on a schedule or in response to events.

---

I want to set up an automated task using GitHub Actions. Here's what I want to automate:

[Describe what you want — for example: "check a website every morning and email me if the price drops", "generate a weekly report from a Google Sheet", "run a script every day at 9am that updates a JSON file in my repo"]

Here's what I need you to do:

1. Create a GitHub Actions workflow file (.github/workflows/something.yml) that does what I described
2. If it runs on a schedule, set up the cron trigger and explain the schedule format in plain English
3. If it needs secrets (API keys, email passwords, etc.), walk me through adding them to the repo settings
4. If it needs to commit changes back to the repo, set up the right permissions
5. Explain how to check if it's running (where to see logs, how to trigger it manually for testing)
6. If something goes wrong, tell me how to debug it

Important: this runs on GitHub's servers for free (for public repos) — I don't need my own computer running. Explain any limits on free usage.
