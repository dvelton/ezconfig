# Build a macOS Menu Bar App

Paste this into your AI tool when you want a small utility that lives in your Mac's menu bar.

---

I want to build a small Mac app that lives in the menu bar (the row of icons at the top right of the screen). When I click the icon, it should show a dropdown with useful information or actions.

Here's what I want it to do:

[Describe what you want — for example: "show my next calendar event", "display the current Bitcoin price", "give me quick access to my most-used links", "show a countdown to a specific date"]

Here's what I need you to do:

1. Build a macOS menu bar app — choose the approach that makes the most sense:
   - SwiftUI (native Mac, best feel, requires Xcode)
   - Python with rumps (simpler, no Xcode needed)
   - Electron with menubar package (if I already have a web-based project)
2. Set up the menu bar icon and dropdown content
3. Make it launch at login if I want (optional)
4. Package it so I can run it like a normal app (double-click to open)
5. Walk me through any setup steps (installing Xcode, Python dependencies, etc.)

I want it to feel like a real Mac utility — small, fast, unobtrusive. Not a full app with a window, just a menu bar icon with a dropdown.
